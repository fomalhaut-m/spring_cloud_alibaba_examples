spring:
  application:
    name: nacos-config-demo
  profiles:
    active: prod
  config:
    import:
      - optional:nacos:${spring.application.name}.yml

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always