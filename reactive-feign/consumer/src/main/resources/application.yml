spring:
  application:
    name: reactive-service-consumer
  profiles:
    active: @spring.profiles.active@

server:
  port: 8085

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000