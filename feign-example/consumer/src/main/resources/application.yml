spring:
  application:
    name: service-consumer
  profiles:
    active: local

server:
  port: 8083

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000