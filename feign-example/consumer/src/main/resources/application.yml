spring:
  application:
    name: service-consumer
  profiles:
    active: @spring.profiles.active@

server:
  port: 8083

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000